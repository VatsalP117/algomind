# Issue tracker

## Tracker type

Local markdown

## Workflow

Issues are created, read, and updated as markdown files in this repo.

- Issues live under `.scratch/<feature>/`
- Each issue is a single markdown file
- Use the feature name (or `misc`) for the directory
- The agent creates files directly; no CLI tool needed
